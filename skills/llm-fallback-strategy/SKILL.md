# llm-fallback-strategy

If LLM access is missing:
- deterministic checks still run
- degraded checks use weaker evidence
- skipped checks must be visible in coverage

A clean report with partial coverage is not a complete audit.
